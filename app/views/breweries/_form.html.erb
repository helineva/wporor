<%= form_with(model: brewery, local: true) do |form| %>
  <% if brewery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brewery.errors.count, "error") %> prohibited this brewery from being saved:</h2>

      <ul>
      <% brewery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name, class:'col-form-label col-sm-2' %>
    <%= form.text_field :name, class:'form-control col-sm-5' %>
  </div>

  <div class="form-group row">
    <%= form.label :year, class:'col-form-label col-sm-2' %>
    <%= form.number_field :year, class:'form-control col-sm-5' %>
  </div>

  <div class="form-check">
    <%= form.check_box :active, class:'form-check-input' %>
    <%= form.label :active, class:'form-check-label' %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
