<%= form_with(model: style, local: true) do |form| %>
  <% if style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(style.errors.count, "error") %> prohibited this style from being saved:</h2>

      <ul>
      <% style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name, class:'col-form-label col-sm-2' %>
    <%= form.text_field :name, class:'form-control col-sm-5' %>
  </div>

  <div class="form-group row">
    <%= form.label :description, class:'col-form-label col-sm-2' %>
    <%= form.text_area :description, class:'form-control col-sm-5' %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
