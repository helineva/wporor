<%= form_with(model: beerclub, local: true) do |form| %>
  <% if beerclub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beerclub.errors.count, "error") %> prohibited this beerclub from being saved:</h2>

      <ul>
      <% beerclub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :name, class:'col-sm-2 col-form-label' %>
    <%= form.text_field :name, class:'form-control col-sm-5' %>
  </div>

  <div class="form-group row">
    <%= form.label :founded, class:'col-sm-2 col-form-label' %>
    <%= form.number_field :founded, class:'form-control col-sm-5' %>
  </div>

  <div class="form-group row">
    <%= form.label :city, class:'col-sm-2 col-form-label' %>
    <%= form.text_field :city, class:'form-control col-sm-5' %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
